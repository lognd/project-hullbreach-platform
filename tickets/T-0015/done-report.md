## Done report

Changed:
- src/hullbreach_server/auth/passwords.py::hash_password / verify_password
  (Argon2id via pwdlib.PasswordHash.recommended(), per section 5 / D4)
- src/hullbreach_server/db/models/user.py::Role / User (table users, per
  section 2: id UUID pk, unique username(32)/email(254), password_hash,
  role Enum(native_enum=False, name="role") default player, created_at)
- src/hullbreach_server/db/models/__init__.py (re-exports Role, User)
- src/hullbreach_server/db/migrations/env.py: imports db.models so
  Base.metadata is populated before autogenerate/compare_metadata
- src/hullbreach_server/db/migrations/versions/0f6d70e4d209_create_users_table.py
  (autogenerated against the User model, hand-cleaned to repo style)
- tests/unit/test_roles.py: two of T-0028's xfail tests (Role enum
  membership, User default role) now genuinely pass once db/models/user.py
  lands (same fixture-activation class as T-0006/T-0007's ripple fixes);
  xfail markers dropped for those two only, the other four (require_admin,
  403/200 routes, schema exclusion) still correctly xfail pending T-0028
- docs/index.md, docs/design/sprint-1.md, design/hullbreach.strata: doc
  anchors, Database migrations section update, tests->auth Flow, and a
  refresh of the auth node's stale SYS113 waiver / REL200 password_hash
  waivers (re-pointed from T-0015 to T-0016, the real persistence caller)
- pyproject.toml / uv.lock: pwdlib[argon2] dependency

Evidence: tests/unit/test_passwords.py::test_hash_password_verifies_and_does_not_store_plaintext
(bound to acceptance [1]), plus test_verify_password_rejects_wrong_password,
test_hash_password_uses_argon2id_scheme,
test_hash_password_is_salted_so_two_hashes_of_the_same_password_differ,
tests/system/test_build.py::test_db_upgrade_head_matches_declarative_metadata,
tests/unit/test_roles.py::test_role_enum_has_exactly_player_and_admin_members,
tests/unit/test_roles.py::test_user_default_role_is_player.

Filed: none new.

T-0099 status: still queued, not folded into T-0007 or this ticket.
T-0007's CLI scope (db upgrade/seed) never builds App/create_app per
design section 4, and T-0015's scope is the User model/passwords helper,
neither touches app/app.py where T-0099's fail-fast wiring belongs.

Gates: `frob check --base origin/main --ticket T-0015` -- 0 errors, 91
warnings, 1 unresolved (gate:FLAGCOV, pre-existing/unrelated), 52 waived.
Exact CI sequence run locally: ruff check/format, ty check, `uv run
pytest tests/ -n auto -q --cov=src --cov-report=term-missing` (91%
coverage, all green); `npx tsc --noEmit`, `npx eslint .`, `npx vitest
run` (36/36, all green).
Waivers: WIRE001 on hash_password/verify_password (follow_up T-0016/
T-0020) and User (follow_up T-0016).

### Changed
```
 tickets/T-0015/ticket.md | 86 +++++++++++++++++++++++++++++++++++++++++++++++-
 1 file changed, 85 insertions(+), 1 deletion(-)
```

### Evidence
- `tests/unit/test_passwords.py::test_hash_password_verifies_and_does_not_store_plaintext` (pytest node id, verified passing when recorded)
- `tests/unit/test_passwords.py::test_verify_password_rejects_wrong_password` (pytest node id, verified passing when recorded)
- `tests/unit/test_passwords.py::test_hash_password_uses_argon2id_scheme` (pytest node id, verified passing when recorded)
- `tests/unit/test_passwords.py::test_hash_password_is_salted_so_two_hashes_of_the_same_password_differ` (pytest node id, verified passing when recorded)
- `tests/system/test_build.py::test_db_upgrade_head_matches_declarative_metadata` (pytest node id, verified passing when recorded)
- `tests/unit/test_roles.py::test_role_enum_has_exactly_player_and_admin_members` (pytest node id, verified passing when recorded)
- `tests/unit/test_roles.py::test_user_default_role_is_player` (pytest node id, verified passing when recorded)

### Captured claims
- tests: 7 passed (from 7 evidence id(s))
- gates: unmeasured (no parsable gate-summary from a fresh check)
